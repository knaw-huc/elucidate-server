package com.digirati.elucidate.converter

object OAToW3CAnnotationCollectionConverter :
    AbstractConverter(
        fieldMappings = HashMap(),
        typeMappings = HashMap(),
        nodeConverterMappings = HashMap()
    ) {
}